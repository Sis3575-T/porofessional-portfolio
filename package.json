{
  "name": "portfolio-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend/*",
    "backend",
    "backend/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,admin -c cyan,green,magenta \"node --watch backend/src/server.js\" \"cd frontend/web && npm run dev\" \"cd frontend/admin && npm run dev\"",
    "dev:all": "concurrently -n api,web,admin -c cyan,green,magenta \"node --watch backend/src/server.js\" \"cd frontend/web && npm run dev\" \"cd frontend/admin && npm run dev\"",
    "dev:backend": "node --watch backend/src/server.js",
    "dev:web": "npm --workspace frontend/web run dev",
    "dev:admin": "npm --workspace frontend/admin run dev",
    "start": "node backend/src/server.js",
    "postinstall": "npx prisma generate --schema=backend/prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "nodemailer": "^6.9.0",
    "prisma": "^5.22.0"
  }
}
